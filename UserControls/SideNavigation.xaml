<UserControl x:Class="TemplateEngine_v3.UserControls.SideNavigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TemplateEngine_v3.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             x:Name="controlGrid">
    <ItemsControl ItemsSource="{Binding MenuList, ElementName=controlGrid}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <RadioButton Style="{StaticResource MaterialDesignTabRadioButton}"
                             MinWidth="60"
                             Foreground="White"
                             IsChecked="{Binding IsSelected}"
                             GroupName="{Binding GroupName}"
                             Cursor="Hand"
                             HorizontalContentAlignment="Left"
                             Command="{Binding OpenPageCommand, ElementName=controlGrid}"
                             CommandParameter="{Binding}">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Stretch">
                        <materialDesign:PackIcon Width="24"
                                                 Height="24"
                                                 Kind="{Binding Icon}"
                                                 Foreground="#FFF"
                                                 VerticalAlignment="Center" />

                        <TextBlock Text="{Binding Title}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                    </StackPanel>
                </RadioButton>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
