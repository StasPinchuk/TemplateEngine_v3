<Page x:Class="TemplateEngine_v3.Views.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:TemplateEngine_v3.UserControls"
      xmlns:local="clr-namespace:TemplateEngine_v3.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      x:Name="usersPage"
      Title="UsersPage">

    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding AlloweUsers, UpdateSourceTrigger=PropertyChanged}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"
                                   Orientation="Horizontal"
                                   HorizontalAlignment="Center"
                                   Margin="8"
                                   ItemWidth="320"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:UserBlock User="{Binding}"
                                            SelectUserCommand="{Binding DataContext.SetUserPermissionsCommand, ElementName=usersPage}"
                                            DeleteAllPermissionCommand="{Binding DataContext.DeleteAllPermissionCommand, ElementName=usersPage}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Style="{StaticResource MaterialDesignFloatingActionSecondaryLightButton}"
                Background="Green"
                BorderBrush="Green"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                Width="35"
                Height="35"
                Margin="25, 10"
                Command="{Binding DataContext.SetUserPermissionsCommand, ElementName=usersPage}">
            <materialDesign:PackIcon Kind="Plus"
                                     Width="25"
                                     Height="25"
                                     Foreground="White"/>
        </Button>
    </Grid>
</Page>
